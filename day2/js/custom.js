$(".sample-content").pxgradient({
  step: 10, // Step Color. The smaller the number, the greater the load. Default: 10
  colors: ["#fc0","#0fc","#00f"], // an array of hex colors.
  dir: "x" // gradient direction. "x" - horizontal,  "y" - vertical
});
